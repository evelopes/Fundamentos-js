const numeros = [100, 200, 300, 400, 500, 600]
for(let i = 0; i < numeros.length; i++ ){
    console.log(i, numeros[i])
}

/* No início i é apenas uma variável que foi atribuido o valor 0, após enquanto i for menor que a quantidade de
itens do ARRAY, ele deve se somar +1 representado pelo i++. 
for faz um laço de repetição, enquanto a atribuição i < array.length for verdadeira. 
Com isso ele vai dando console.log de i que inicialmente é 0, e array[0], após primeiro laço os números se alteram
então i = 1 e o array[1] até finalizar a execução de todo array


*/


alice = 0
bob = 0
if a[0] > b[0]:
alice += 1
